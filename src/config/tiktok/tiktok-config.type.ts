export interface TikTokConfig {
  clientId: string;
  clientSecret: string;
  // redirectUri: string;
  baseUrl: string;
  authUrl: string;
  tokenUrl: string;
  tokenRefreshUrl: string;
  apiVersion: string;
}
